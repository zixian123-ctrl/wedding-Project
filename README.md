# 婚礼邀请函 H5

双击 `index.html` 即可预览。为了获得完整的音乐、分享和地图跳转体验，建议通过本地静态服务器运行：

```powershell
python -m http.server 8080
```

随后访问 `http://localhost:8080`。

照片上传、回执、祝福和音乐状态使用浏览器本地存储，不会上传到服务器。封面相机按钮可更换背景，长按或右键可恢复默认封面。
